'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
      <button  className='btn glass'onClick={()=>console.log("clcik")}>Add</button>
    </div>
  )
}

export default AddToCart
